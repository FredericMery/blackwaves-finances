export const metadata = {
  title: "Contact | BlackWaves Cheer",
  description: "Contactez le club BlackWaves Cheer (Marseille).",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
